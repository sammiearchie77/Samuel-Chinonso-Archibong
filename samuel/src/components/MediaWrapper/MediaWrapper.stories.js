import { MediaWrapper } from "./MediaWrapper";

export default {
    title: "Components/MediaWrapper",
    component: MediaWrapper,
};

export const Default = {
    args: {
        className: {},
    },
};